__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
build/
dist/
.env
*.swp

# Persistence: the runtime save overlay (Phase 5) is local state, never committed —
# the authored world lives in game/world/world.json; this is only the mutable delta.
world.save.json
world.save.json.bak
world.save.json.tmp
*.save.json
*.save.json.bak
*.save.json.tmp
# SQLite-backed memory (slice 1b) — local runtime state, never committed.
world.memory.db
*.memory.db
*.memory.db-wal
*.memory.db-shm
# MkDocs build output (the docs site is built by CI, never committed).
site/
